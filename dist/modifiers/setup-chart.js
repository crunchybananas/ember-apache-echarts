import { modifier } from 'ember-modifier';

var setupChart = modifier(function setupChart(element, [setupAction]) {
  setupAction(element);
});

export { setupChart as default };
//# sourceMappingURL=setup-chart.js.map
